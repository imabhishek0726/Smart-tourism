PORT=5000

# Get a free key at https://openweathermap.org/api
OPENWEATHER_API_KEY=

# Get a free key at https://www.geoapify.com/
GEOAPIFY_API_KEY=

# If either key is missing, the app automatically falls back to mock data
# so the demo always works even with no internet / no keys.
